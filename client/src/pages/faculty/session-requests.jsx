import React, { useEffect, useState } from 'react';
import { Check, LoaderCircle, X } from 'lucide-react';
import { facultyService } from '@services/api.service';

export default function SessionRequests() { const [sessions, setSessions] = useState([]); const [loading, setLoading] = useState(true); const [message, setMessage] = useState(''); useEffect(() => { facultyService.getRequests().then((res) => setSessions(res.data || res)).finally(() => setLoading(false)); }, []); const update = async (id, action) => { try { const result = await facultyService.updateRequest(id, { action }); setSessions((current) => current.map((session) => session._id === id ? (result.data || session) : session)); } catch (error) { setMessage(error.response?.data?.message || 'Could not update request.'); } }; if (loading) return <div className="flex min-h-[300px] items-center justify-center"><LoaderCircle className="animate-spin text-teal-700" /></div>; return <section className="space-y-6"><header><p className="text-xs font-semibold uppercase tracking-wider text-teal-700">Faculty workspace</p><h1 className="mt-1 text-3xl font-bold text-ink">Session requests</h1></header>{message && <p className="rounded-xl bg-red-50 p-4 text-sm text-red-700">{message}</p>}<div className="space-y-3">{sessions.map((session) => <article key={session._id} className="flex flex-col gap-4 rounded-2xl border border-slate-200 bg-white p-5 shadow-panel sm:flex-row sm:items-center sm:justify-between"><div><p className="font-bold text-ink">{session.subject} · {session.topic || 'Doubt session'}</p><p className="mt-1 text-sm text-slate-500">{session.student?.name} · {new Date(session.scheduled_at).toLocaleString()}</p><p className="mt-1 text-sm text-slate-600">{session.description}</p></div><div className="flex items-center gap-2">{session.status === 'pending' ? <><button type="button" onClick={() => update(session._id, 'confirm')} className="inline-flex items-center gap-1 rounded-lg bg-teal-700 px-3 py-2 text-xs font-semibold text-white"><Check size={14} /> Confirm</button><button type="button" onClick={() => update(session._id, 'decline')} className="inline-flex items-center gap-1 rounded-lg bg-rose-50 px-3 py-2 text-xs font-semibold text-rose-700"><X size={14} /> Decline</button></> : <span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-semibold capitalize text-slate-600">{session.status}</span>}</div></article>)}{sessions.length === 0 && <p className="rounded-xl border border-dashed border-slate-300 p-8 text-center text-sm text-slate-500">No session requests.</p>}</div></section>; }
