import React, { useEffect, useState } from 'react';
import { CalendarCheck, LoaderCircle, Users } from 'lucide-react';
import { facultyService } from '@services/api.service';

export default function FacultyDashboard() { const [data, setData] = useState(null); const [loading, setLoading] = useState(true); useEffect(() => { facultyService.getDashboard().then((res) => setData(res.data || res)).finally(() => setLoading(false)); }, []); if (loading) return <div className="flex min-h-[300px] items-center justify-center"><LoaderCircle className="animate-spin text-teal-700" /></div>; return <section className="space-y-6"><header><p className="text-xs font-semibold uppercase tracking-wider text-teal-700">Faculty workspace</p><h1 className="mt-1 text-3xl font-bold text-ink">Faculty dashboard</h1><p className="mt-1 text-sm text-slate-500">Manage your availability and respond to student support requests.</p></header><div className="grid gap-4 sm:grid-cols-3"><div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-panel"><Users className="text-teal-700" /><p className="mt-4 text-xs font-semibold uppercase tracking-wider text-slate-400">Pending requests</p><p className="mt-1 text-3xl font-bold text-ink">{data?.pending_requests || 0}</p></div><div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-panel"><CalendarCheck className="text-indigo-700" /><p className="mt-4 text-xs font-semibold uppercase tracking-wider text-slate-400">Today</p><p className="mt-1 text-3xl font-bold text-ink">{data?.today_sessions?.length || 0}</p></div><div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-panel"><p className="text-xs font-semibold uppercase tracking-wider text-slate-400">Availability</p><p className="mt-2 text-xl font-bold text-ink">{data?.availability?.is_available === false ? 'Unavailable' : 'Available'}</p></div></div><div className="rounded-2xl border border-slate-200 bg-white p-6 shadow-panel"><h2 className="text-lg font-bold text-ink">Today’s confirmed sessions</h2><div className="mt-4 space-y-3">{(data?.today_sessions || []).map((session) => <div key={session._id} className="flex justify-between rounded-xl bg-slate-50 p-4 text-sm"><span className="font-semibold text-ink">{session.student?.name} · {session.subject}</span><span className="text-slate-500">{new Date(session.scheduled_at).toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' })}</span></div>)}{!data?.today_sessions?.length && <p className="text-sm text-slate-500">No confirmed sessions today.</p>}</div></div></section>; }
