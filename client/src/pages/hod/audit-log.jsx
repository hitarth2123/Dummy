import React, { useEffect, useState } from 'react';
import api from '@services/api.service';

const AuditLog = () => { const [logs, setLogs] = useState([]); useEffect(() => { api.get('/hod/audit-log').then((result) => setLogs(result.data.data || [])); }, []); return <section className="space-y-6"><div><p className="text-sm font-semibold uppercase tracking-[0.16em] text-teal-700">Governance</p><h1 className="mt-2 text-3xl font-semibold tracking-tight text-ink">Audit log</h1><p className="mt-2 text-slate-600">Read-only activity for your department.</p></div><div className="overflow-x-auto rounded-2xl border border-slate-200 bg-white shadow-panel"><table className="w-full text-left text-sm"><thead className="border-b bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="p-4">Timestamp</th><th className="p-4">Student ID</th><th className="p-4">Request</th><th className="p-4">Flag</th><th className="p-4">Status</th></tr></thead><tbody>{logs.map((log) => <tr key={`${log.timestamp}-${log.action}`} className="border-b last:border-0"><td className="p-4">{new Date(log.timestamp).toLocaleString()}</td><td className="p-4">{log.student_id || '-'}</td><td className="p-4">{log.request_summary}</td><td className="p-4">{log.flag_category || '-'}</td><td className="p-4 capitalize">{log.status}</td></tr>)}</tbody></table></div></section>; };
export default AuditLog;
