import React, { useEffect, useState } from 'react';
import { CalendarPlus, LoaderCircle } from 'lucide-react';
import { Link, useLocation } from 'react-router-dom';
import { studentService } from '@services/api.service';

export default function MySessions() { const location = useLocation(); const [sessions, setSessions] = useState([]); const [loading, setLoading] = useState(true); useEffect(() => { studentService.getSessions().then((res) => setSessions(res.data || res)).finally(() => setLoading(false)); }, []); if (loading) return <div className="flex min-h-[300px] items-center justify-center"><LoaderCircle className="animate-spin text-primary" /></div>; return <section className="space-y-6"><header className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-semibold uppercase tracking-wider text-primary">Faculty support</p><h1 className="mt-1 text-3xl font-bold text-on-surface">My sessions</h1><p className="mt-1 text-sm text-on-surface-variant">Track requests, confirmations, and meeting links.</p></div><Link to="/student/book-session" className="inline-flex items-center justify-center gap-2 rounded-xl bg-primary-container px-4 py-2.5 text-sm font-semibold text-white hover:bg-inverse-primary"><CalendarPlus size={16} /> Book a session</Link></header>{location.state?.success && <p className="rounded-xl border border-emerald-200 bg-secondary-container/10 p-4 text-sm font-medium text-emerald-300">{location.state.success}</p>}<div className="space-y-3">{sessions.map((session) => <Link key={session._id} to={`/student/my-sessions/${session._id}`} className="block rounded-2xl border border-surface-variant/40 bg-surface-container-low p-5 shadow-panel transition hover:border-teal-300 hover:shadow-lg"><div className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="font-bold text-on-surface">{session.subject} · {session.topic || 'Doubt session'}</h2><p className="mt-1 text-sm text-on-surface-variant">{session.faculty?.name} · {new Date(session.scheduled_at).toLocaleString()}</p></div><span className="rounded-full bg-surface-container-high px-3 py-1 text-xs font-semibold capitalize text-on-surface-variant">{session.status}</span></div><p className="mt-3 text-xs font-semibold text-primary">Open session details →</p></Link>)}{sessions.length === 0 && <div className="rounded-xl border border-dashed border-surface-variant/50 p-8 text-center"><p className="text-sm text-on-surface-variant">No sessions booked yet.</p><Link to="/student/book-session" className="mt-4 inline-flex items-center gap-2 rounded-xl bg-primary-container px-4 py-2.5 text-sm font-semibold text-white hover:bg-inverse-primary"><CalendarPlus size={16} /> Book your first session</Link></div>}</div></section>; }
