import React, { useEffect, useState } from 'react';
import { CalendarPlus, LoaderCircle } from 'lucide-react';
import { Link, useLocation } from 'react-router-dom';
import { studentService } from '@services/api.service';

export default function MySessions() { const location = useLocation(); const [sessions, setSessions] = useState([]); const [loading, setLoading] = useState(true); useEffect(() => { studentService.getSessions().then((res) => setSessions(res.data || res)).finally(() => setLoading(false)); }, []); if (loading) return <div className="flex min-h-[300px] items-center justify-center"><LoaderCircle className="animate-spin text-teal-700" /></div>; return <section className="space-y-6"><header className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-semibold uppercase tracking-wider text-teal-700">Faculty support</p><h1 className="mt-1 text-3xl font-bold text-ink">My sessions</h1><p className="mt-1 text-sm text-slate-500">Track requests, confirmations, and meeting links.</p></div><Link to="/student/book-session" className="inline-flex items-center justify-center gap-2 rounded-xl bg-teal-700 px-4 py-2.5 text-sm font-semibold text-white hover:bg-teal-800"><CalendarPlus size={16} /> Book a session</Link></header>{location.state?.success && <p className="rounded-xl border border-emerald-200 bg-emerald-50 p-4 text-sm font-medium text-emerald-800">{location.state.success}</p>}<div className="space-y-3">{sessions.map((session) => <Link key={session._id} to={`/student/my-sessions/${session._id}`} className="block rounded-2xl border border-slate-200 bg-white p-5 shadow-panel transition hover:border-teal-300 hover:shadow-lg"><div className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="font-bold text-ink">{session.subject} · {session.topic || 'Doubt session'}</h2><p className="mt-1 text-sm text-slate-500">{session.faculty?.name} · {new Date(session.scheduled_at).toLocaleString()}</p></div><span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-semibold capitalize text-slate-600">{session.status}</span></div><p className="mt-3 text-xs font-semibold text-teal-700">Open session details →</p></Link>)}{sessions.length === 0 && <div className="rounded-xl border border-dashed border-slate-300 p-8 text-center"><p className="text-sm text-slate-500">No sessions booked yet.</p><Link to="/student/book-session" className="mt-4 inline-flex items-center gap-2 rounded-xl bg-teal-700 px-4 py-2.5 text-sm font-semibold text-white hover:bg-teal-800"><CalendarPlus size={16} /> Book your first session</Link></div>}</div></section>; }
